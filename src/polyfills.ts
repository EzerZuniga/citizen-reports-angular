/**
 * Polyfills for Angular app.
 * Loaded before the app. Keep minimal — add only what is necessary.
 */

// Example: If you need global fetch/polyfills for older browsers, add here.
// import 'core-js/features/array/includes';

// No-op polyfills file (keeps Angular build happy).

export {};
